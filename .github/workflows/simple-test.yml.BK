name: Simple Runner Test

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build_job_1:
    runs-on: [self-hosted, dev]
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6
      - name: Echo Hello World
        run: |
          echo " The self-hosted runner is active and working."
          ls -la
          cat README.md

  test_job_2:
    runs-on: [self-hosted, dev]
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6
      - name: Echo Hello World
        run: |
          echo " The self-hosted runner is active and working."
          ls -la
          cat README.md
          
  deploy_job_3:
    runs-on: [self-hosted, dev]
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6
      - name: Echo Hello World
        run: |
          echo " The self-hosted runner is active and working."
          ls -la
          cat README.md        
      

          
